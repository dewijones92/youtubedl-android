# Sourced by ci/quality-check.sh — this repo's quality-gate settings.
# Main sources only: tests may legitimately repeat setup code.
SRC_DIRS=(app/src/main aria2c/src/main buildSrc/src/main common/src/main ffmpeg/src/main library/src/main ytdlp-kt/src/main)
CPD_LANGUAGES=(java kotlin)
